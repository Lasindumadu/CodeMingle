# spring.application.name=codemingle-backend

spring.datasource.url=jdbc:mysql://localhost:3306/CodeMingle?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Lasindu123

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Ensure schema is created/updated on startup
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true

# Load schema and data files
# spring.datasource.init-mode=always
spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:schema_mysql.sql
spring.sql.init.data-locations=classpath:data_mysql.sql

# JWT Configuration
jwt.secret=mySecretKey123456789012345678901234567890123456789012345678901234567890
jwt.expiration=86400000

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
